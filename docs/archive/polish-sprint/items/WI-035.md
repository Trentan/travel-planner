# WI-035: Map Geocoding & Marker Refinement

## Status
- **Priority**: High
- **Status**: Done
- **Completed**: 2026-05-20
- **Assigned**: Codex
- **Before screenshot**: [WI-035-before.png](../screenshots/before/WI-035-before.png)
- **After proposal**: [WI-035-proposal.png](proposals/WI-035-proposal.png)
- **After screenshot**: [WI-035-after-2.png](../screenshots/after/WI-035-after-2.png)


## Goal
Improve the journey map's city mapping reliability and simplify visual markers.

## Requirements
1. **Online Geocoding Fallback**: Implement a search function that queries an online API (like Nominatim) if a city is not found in the local database.
2. **IATA & Metadata Enrichment**: Use geocoding results to try and find/confirm IATA codes.
3. **Simplified Markers**: Markers should only display the first sequence number of a visit to reduce visual clutter. 
4. **Directional Flow**: Ensure polylines clearly demonstrate the back-and-forth travel path.
5. **Offline-First**: Ensure online-found coordinates are saved locally to maintain offline support.

## Tasks
- [x] Implement `searchCityOnline` utility in `js/data.js`.
- [x] Add "Search Online" button to Manage Cities dialog.
- [x] Refactor `js/map.js` to simplify marker numbering (show only first visit).
- [x] Update `js/map.js` marker popups to show full visit history (e.g., "First visit: 1, also visited at: 5").
- [x] Validate routing with return legs.
- [x] See review notes

## Technical Notes
- API: Nominatim (OpenStreetMap) for free, no-key geocoding (respecting rate limits).
- Storage: Coordinates fetched online must be saved to `citiesData`.

## Review Notes

- 2026-05-18: no still wrong, not per the trip - the cities should be in leg order and per the city submenu an numbered accordingly - eg the map should be bolzana, verona, milan per my trip...  also noticed city numbers
missing, they should only be in increments of 1 with no numbers missing
- 2026-05-20: Map marker numbering is now right, but the map route lines still connect the path incorrectly; for example the polyline goes to Milan before Verona. To do: make the line path follow the trip/city submenu order too. Also add a "fetch all missing locations" action that is only visible when one or more cities have no stored coordinates, fills those locations automatically, and stores them in the trip JSON. Longer-term, city coordinates should be captured at city insertion time whenever possible.

## After

Latest review screenshot: [WI-035-after-2.png](../screenshots/after/WI-035-after-2.png)

Supplemental Cities dialog screenshot: [WI-035-after-2-cities.png](../screenshots/after/WI-035-after-2-cities.png)

The map now uses the same canonical city sequence as the city submenu for marker numbering and route lines. Transit journeys can add missing transit-only cities, but they no longer pull real destination cities out of trip order. The rendered marker labels are contiguous 1-15 with no skipped numbers, and the Italian sequence is Bolzano, Verona, Milan. Cities with missing stored coordinates can be filled in bulk from the Cities dialog, and newly added cities attempt to capture coordinates at insertion time.

## Verification

- Passed: `node --check js/data.js`.
- Passed: `node --check js/itinerary.js`.
- Passed: `node --check js/map.js`.
- Passed: `node tests/city-nav-regression.js`.
- Passed: desktop Playwright verification at 1440x900 with real `backups/2026_June_July_Europe_Thailand.json` data. Confirmed city nav order and map legend order both include `Bolzano`, `Verona`, `Milan`, and marker labels are contiguous `1` through `15`.
- Passed: desktop Playwright verification confirmed the route polyline sequence includes `Bolzano`, `Verona`, `Milan` and then later returns through `Koh Samui`, `Bangkok`, `Brisbane`.
- Passed: Cities dialog Playwright verification confirmed the bulk missing-location button appears only while cities are missing stored coordinates, and fills all missing trip city coordinates into `travelApp_cities_v1`.

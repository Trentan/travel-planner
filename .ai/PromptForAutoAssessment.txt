Read .ai/ORCHESTRATOR.md and .ai/RUN_AUTONOMOUS.md.

Act as the Project Orchestrator for the "v1.0.0-google-play" release. Run the full pipeline autonomously from start to finish following these exact steps:

--- PHASE 1: AUDIT ---
1. Assume the QA UX Agent role (.ai/QA_UX_AGENT.md).
2. Audit the application by inspecting code, running static checks, and executing local tests.
3. For every bug or UX issue found, create a GitHub issue using `gh issue create` tagged with the milestone "v1.0.0-google-play" and appropriate priority labels (critical, high, medium, low).

--- PHASE 2: FIX & HARDEN ---
4. Check open issues using `gh issue list --milestone "v1.0.0-google-play"`.
5. Switch to the Developer Agent role (.ai/DEVELOPER_AGENT.md).
6. Pick issues in priority order (critical -> high -> medium -> low).
7. For each issue: apply the fix, verify locally with build/test runner, commit the changes, and close the issue using `gh issue close`.
8. Loop until 0 critical/high issues remain (or max 3 retries per issue, applying circuit breaker rules).

--- PHASE 3: RELEASE VALIDATION ---
9. Switch to the Release Agent role (.ai/RELEASE_AGENT.md).
10. Check milestone progress and run a full production build locally.
11. Audit against docs/RELEASE_CHECKLIST.md and output docs/RELEASE_REPORT.md with a final GO/NO-GO determination and readiness score.

Do not stop to ask for human confirmation between phases. Proceed continuously until Phase 3 generates docs/RELEASE_REPORT.md or a fatal execution error occurs.